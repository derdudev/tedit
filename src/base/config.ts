// TODO: implement debug funcitons (maybe a logger class) and the debug key to allow switching debug mesages on and off very fast

const globalConfig = {
    debug: {
        stage: true,
        navbarLoading: false,
        initText: false,
        renderSelectionCheck: false,
        twoDigitShortcut: true,
        setSelection: true,
        deletingInfo: true,
    },
};

export default globalConfig;